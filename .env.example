# ─────────────────────────────────────────────────
#  FreeChatCU — Environment Variables
# ─────────────────────────────────────────────────
# These are set automatically when you link Vercel KV
# to your project (Storage → Create → KV Database).
# You do NOT need to copy these manually.

KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
